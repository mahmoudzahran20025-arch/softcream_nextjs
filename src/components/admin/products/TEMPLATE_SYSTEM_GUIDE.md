# 🎨 Template System Guide - Frontend

> آخر تحديث: December 4, 2025
> الغرض: توثيق نظام القوالب وملفاته في الفرونت

---

## 📊 نظرة عامة

`template_id` هو **المتحكم الوحيد** في شكل عرض المنتج.

### القوالب المتاحة:

| template_id | الاسم | الاستخدام | التعقيد |
|-------------|-------|-----------|---------|
| `template_1` | بسيط | منتجات بدون خيارات | ⭐ |
| `template_2` | متوسط | منتجات مع خيارات محدودة | ⭐⭐ |
| `template_3` | معقد (BYO) | منتجات قابلة للتخصيص الكامل | ⭐⭐⭐ |

---

## 📁 ملفات كل قالب

### Template 1 (بسيط):

```
البطاقة (Card):
└── src/components/ui/cards/SimpleCard.tsx

الـ Modal:
└── إضافة مباشرة للسلة (بدون modal)

السلوك:
- يعرض المنتج مع زر "أضف للسلة"
- لا يفتح modal
- يضيف المنتج مباشرة
```

### Template 2 (متوسط):

```
البطاقة (Card):
└── src/components/ui/cards/StandardProductCard.tsx

الـ Modal:
└── src/components/modals/ProductModal/templates/builders/MediumTemplate.tsx

السلوك:
- يعرض المنتج مع زر "اختر"
- يفتح modal لاختيار الخيارات
- خطوات محدودة (1-2 خطوة)
```

### Template 3 (معقد - BYO):

```
البطاقة (Card):
└── src/components/ui/cards/BYOProductCard.tsx

الـ Modal:
└── src/components/modals/ProductModal/templates/builders/ComplexTemplate.tsx

السلوك:
- يعرض المنتج مع زر "ابني طلبك"
- يفتح modal متعدد الخطوات
- خطوات كثيرة (3+ خطوات)
```

---

## 🔄 تدفق العرض

### 1. تحديد البطاقة:

```typescript
// src/components/ui/ProductCard.tsx

const getCardComponent = (templateId: string) => {
  switch (templateId) {
    case 'template_1':
      return SimpleCard;
    case 'template_2':
      return StandardProductCard;
    case 'template_3':
      return BYOProductCard;
    default:
      return StandardProductCard; // fallback
  }
};
```

### 2. تحديد الـ Modal:

```typescript
// src/components/modals/ProductModal/templates/ProductTemplateRenderer.tsx

const getTemplateComponent = (templateId: string) => {
  switch (templateId) {
    case 'template_1':
      return null; // لا modal
    case 'template_2':
      return MediumTemplate;
    case 'template_3':
      return ComplexTemplate;
    default:
      return MediumTemplate; // fallback
  }
};
```

---

## 🎯 متى تستخدم كل قالب؟

### Template 1 (بسيط):
- ✅ منتجات جاهزة بدون خيارات
- ✅ مشروبات معلبة
- ✅ حلويات جاهزة
- ❌ لا يصلح لمنتجات قابلة للتخصيص

### Template 2 (متوسط):
- ✅ منتجات مع خيارات محدودة (1-2 مجموعة)
- ✅ ميلك شيك مع اختيار النكهة
- ✅ آيس كريم مع اختيار الحجم
- ❌ لا يصلح لمنتجات معقدة

### Template 3 (معقد - BYO):
- ✅ منتجات قابلة للتخصيص الكامل
- ✅ سوفت سيرف مع كل الخيارات
- ✅ Build Your Own
- ❌ قد يكون معقداً للمنتجات البسيطة

---

## 📋 Checklist للأدمن

عند إضافة منتج جديد:

1. [ ] حدد نوع المنتج (بسيط/متوسط/معقد)
2. [ ] اختر القالب المناسب
3. [ ] أضف مجموعات الخيارات (إذا لزم)
4. [ ] تحقق من المعاينة

---

## ⚠️ تنبيهات للـ Agent

### الحقول المهملة (لا تستخدمها):

```typescript
// ❌ لا تستخدم هذه الحقول
product_type   // مهمل
card_style     // مهمل
layout_mode    // مهمل

// ✅ استخدم هذا فقط
template_id    // المتحكم الوحيد
```

### عند تعديل القوالب:

1. تأكد من تحديث **كل** الملفات المرتبطة
2. اختبر على جميع أحجام الشاشات
3. تحقق من أن الـ fallback يعمل

---

## 🔗 ملفات مرتبطة

| الملف | الوظيفة |
|-------|---------|
| `ARCHITECTURE.md` | هندسة الفرونت العامة |
| `softcream-api/schema.sql` | Schema الجداول |
| `softcream-api/src/routes/admin/PRODUCTS_OPTIONS_RELATIONSHIPS.md` | علاقات الباك اند |
