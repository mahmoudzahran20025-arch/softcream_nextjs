# 🎯 دليل نظام التخصيص الجديد (Everything-as-Option)

## 📋 نظرة عامة

النظام الجديد يعتمد على **مجموعات الخيارات (Option Groups)** بدلاً من النظام القديم.

### ❌ النظام القديم (تم إلغاؤه):
- `is_customizable` - عمود في جدول المنتجات
- `allowed_addons` - قائمة الإضافات المسموحة
- جدول `addons` - الإضافات المنفصلة

### ✅ النظام الجديد:
- `option_groups` - مجموعات الخيارات (حاويات، مقاسات، نكهات...)
- `options` - الخيارات داخل كل مجموعة
- `product_options` - ربط المنتجات بمجموعات الخيارات

---

## 🔄 خطوات إضافة منتج جديد

### الخطوة 1: إضافة المنتج الأساسي
1. اذهب إلى **صفحة المنتجات**
2. اضغط **"إضافة منتج جديد"**
3. املأ البيانات الأساسية:
   - الاسم (عربي/إنجليزي)
   - السعر الأساسي
   - الفئة
   - الصورة
   - معلومات التغذية

### الخطوة 2: اختيار نوع المنتج
اضغط على ⚙️ (إعدادات التخصيص) واختر النوع:

| النوع | الوصف | مثال |
|-------|-------|------|
| `standard` | منتج بسيط بدون تخصيص | بستاشيو فاخر |
| `byo_ice_cream` | آيس كريم قابل للتخصيص بالكامل | كوب مخصص |
| `preset_ice_cream` | آيس كريم بوصفة جاهزة + إضافات | فانيليا كلاسيك |
| `milkshake` | ميلك شيك مع خيارات | ميلك شيك شوكولاتة |
| `dessert` | حلويات مع آيس كريم | براوني فدج |

### الخطوة 3: ربط الخيارات
من نفس النافذة (ConfigModal):

#### للـ BYO Ice Cream:
- ✅ اختر الحاويات (كوب، كون، وافل)
- ✅ اختر المقاسات (صغير، وسط، كبير)
- ✅ أضف مجموعات التخصيص (نكهات، صوصات، إضافات)

#### للـ Preset Ice Cream:
- ❌ لا حاجة للحاويات (المنتج جاهز)
- ❌ لا حاجة للمقاسات
- ✅ أضف صوصات وإضافات اختيارية

#### للـ Milkshake:
- ❌ لا حاجة للحاويات
- ✅ اختر المقاسات
- ✅ أضف إضافات الميلك شيك

#### للـ Dessert:
- ❌ لا حاجة للحاويات/مقاسات
- ✅ أضف آيس كريم اختياري
- ✅ أضف صوصات

---

## 📦 مجموعات الخيارات المتاحة

| المجموعة | الأيقونة | الوصف |
|----------|----------|-------|
| `containers` | 🥤 | الحاويات (كوب، كون، وافل) |
| `sizes` | 📏 | المقاسات (صغير، وسط، كبير) |
| `flavors` | 🍦 | النكهات (فانيليا، شوكولاتة...) |
| `sauces` | 🫗 | الصوصات (شوكولاتة، كراميل...) |
| `dry_toppings` | 🍪 | الإضافات الجافة (أوريو، لوتس...) |
| `milkshake_addons` | 🥤 | إضافات الميلك شيك |
| `dessert_ice_cream` | 🍨 | آيس كريم للحلويات |

---

## ⚙️ إعدادات قواعد التخصيص

لكل مجموعة خيارات مربوطة بمنتج:

| الإعداد | الوصف | مثال |
|---------|-------|------|
| `is_required` | هل الاختيار إجباري؟ | النكهات إجبارية |
| `min_selections` | الحد الأدنى للاختيارات | 1 نكهة على الأقل |
| `max_selections` | الحد الأقصى للاختيارات | 2 نكهات كحد أقصى |
| `display_order` | ترتيب العرض | 1, 2, 3... |

---

## 🎨 كيف يظهر في الفرونت إند؟

### ProductModal يعرض تلقائياً:
1. **ContainerSelector** - لو `hasContainers = true`
2. **SizeSelector** - لو `hasSizes = true`
3. **OptionsGrid** - لو `hasCustomization = true`

### الحساب التلقائي:
```
السعر النهائي = السعر الأساسي 
              + سعر الحاوية 
              + سعر المقاس 
              + مجموع أسعار الخيارات المختارة
```

---

## 🔧 إدارة الخيارات

### من صفحة "إعدادات التخصيص":
1. **إضافة حاوية جديدة** - مع السعر والتغذية
2. **إضافة مقاس جديد** - مع معامل التغذية
3. **إضافة مجموعة خيارات** - مع الأيقونة والوصف
4. **إضافة خيار** - داخل مجموعة معينة

---

## ✅ ملخص سريع

```
المنتج ← product_type ← product_options ← option_groups ← options
```

1. **أضف المنتج** بالبيانات الأساسية
2. **اختر النوع** (byo, preset, milkshake, dessert, standard)
3. **اربط الخيارات** من ConfigModal
4. **النظام يحسب تلقائياً** السعر والتغذية

---

## 🚀 نصائح

- ✅ استخدم `product_type` لتحديد شكل الكارت في الفرونت
- ✅ الخيارات المشتركة (صوصات، إضافات) يمكن ربطها بعدة منتجات
- ✅ التغذية تُحسب تلقائياً من الخيارات المختارة
- ✅ لا حاجة لـ `is_customizable` - النظام يحدد تلقائياً من البيانات
