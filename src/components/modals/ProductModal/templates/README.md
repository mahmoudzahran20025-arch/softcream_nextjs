# Product Templates

## 📁 هيكل الملفات

```
templates/
├── index.ts                    # Barrel exports
├── ProductTemplateRenderer.tsx # المنسق الرئيسي - يختار الـ template المناسب
├── BYOTemplate.tsx            # 🥤 الكوب المخصص (Build Your Own)
├── DessertTemplate.tsx        # 🍰 الحلويات
├── MilkshakeTemplate.tsx      # 🥤 الميلك شيك
├── PresetTemplate.tsx         # 🍨 الآيس كريم الجاهز
├── StandardTemplate.tsx       # 🍽️ المنتجات العادية
└── README.md                  # هذا الملف
```

## 🎯 كيف يعمل النظام

1. `ProductTemplateRenderer` يستقبل `productConfig` من `useProductConfiguration`
2. يحدد نوع المنتج (`productType`)
3. يعرض الـ template المناسب

## 📋 أنواع الـ Templates

### 1. BYOTemplate (الكوب المخصص)
- **الاستخدام:** منتجات `byo_ice_cream` أو أي منتج له containers + customization
- **الخطوات:**
  1. اختيار الحاوية (كوب/كون/وافل)
  2. اختيار المقاس
  3. اختيار النكهات (إجباري)
  4. إضافات اختيارية

**ملاحظة UX:** النكهات لا تعرض "مجاناً" لأنها جزء أساسي من المنتج

### 2. DessertTemplate (الحلويات)
- **الاستخدام:** منتجات `dessert` (وافل، براوني، إلخ)
- **الأقسام:**
  - اختيار الحجم
  - إضافة آيس كريم (اختياري - قسم مميز)
  - صوصات
  - تبينجز

### 3. MilkshakeTemplate (الميلك شيك)
- **الاستخدام:** منتجات `milkshake`
- **الأقسام:**
  - Header مميز بلون برتقالي
  - اختيار الحجم
  - إضافات

### 4. PresetTemplate (الآيس كريم الجاهز)
- **الاستخدام:** منتجات `preset_ice_cream` (نكهات جاهزة)
- **الأقسام:**
  - اختيار الحجم
  - إضافات اختيارية
  - لا يعرض اختيار النكهات (المنتج له نكهة محددة)

### 5. StandardTemplate (المنتجات العادية)
- **الاستخدام:** أي منتج آخر
- **الأقسام:**
  - اختيار الحجم (إن وجد)
  - مجموعات التخصيص

## 🔧 كيفية إضافة Template جديد

1. أنشئ ملف جديد: `NewTemplate.tsx`
2. استخدم نفس الـ interface:
```typescript
interface NewTemplateProps {
  product: any
  sizes: any[]
  selectedSize: any
  onSizeSelect: (size: any) => void
  customizationRules: CustomizationGroup[]
  selections: Record<string, string[]>
  onSelectionChange: (groupId: string, ids: string[]) => void
}
```
3. أضف الـ export في `index.ts`
4. أضف الشرط في `ProductTemplateRenderer.tsx`

## 🎨 قواعد UX

1. **لا تعرض "مجاناً" للعناصر الأساسية** - النكهات في BYO جزء من المنتج
2. **استخدم خطوات واضحة** - رقم + أيقونة + عنوان
3. **أظهر التقدم** - عدد الاختيارات / الحد الأقصى
4. **رسائل تحقق واضحة** - إذا لم يختر العميل العناصر الإجبارية
5. **ألوان مميزة لكل نوع:**
   - BYO: Pink/Purple gradient
   - Milkshake: Amber/Orange
   - Dessert: Pink with ice cream highlight
   - Preset: Cyan
   - Standard: Slate/Neutral

## 📝 ملاحظات التطوير

- كل template مستقل ويمكن تعديله بدون التأثير على الآخرين
- `CustomizationSummary` مشترك بين كل الـ templates (يعرض في `ProductTemplateRenderer`)
- الـ animations تستخدم `framer-motion`
