# 🧪 اختبر الآن - خطوات بسيطة

## ✅ الـ Server يعمل على:
```
http://localhost:3000
```

## 🎯 الخطوات (3 خطوات فقط):

### 1. افتح المنتج
```
http://localhost:3000?product=soft_serve_cup
```

### 2. افتح Console
```
اضغط F12
اذهب إلى Console
```

### 3. ابحث عن هذه الرسائل:

#### ✅ إذا رأيت:
```
🎨 Fetching customization rules for product soft_serve_cup...
   isOpen: true
   enabled: true
✅ Loaded 3 customization groups
🔍 useCustomization State: {
  isCustomizable: true,
  rulesCount: 3
}
```

**معنى ذلك:** يعمل! قسم التخصيص يجب أن يظهر في الصفحة.

#### ❌ إذا لم ترَ أي رسائل:
**معنى ذلك:** الكود القديم لا يزال يعمل (cache).

**الحل:**
```
1. اضغط Ctrl + Shift + R (تحديث قوي)
2. أو افتح في Incognito Mode (Ctrl + Shift + N)
```

#### ⚠️ إذا رأيت:
```
🔍 useCustomization State: {
  isCustomizable: false,
  rulesCount: 0
}
```

**معنى ذلك:** الـ API لا يرجع بيانات.

**الحل:**
```powershell
cd softcream-api
.\make-product-customizable.ps1 -ProductId "soft_serve_cup"
```

## 🎨 ما يجب أن تراه في الصفحة:

```
┌─────────────────────────────────────┐
│ Custom Soft Serve Cup               │
│ 45 ج.م                              │
├─────────────────────────────────────┤
│ ✨ اصنع الآيس كريم الخاص بك         │  ← يجب أن يظهر هذا!
│                                     │
│ 🍦 النكهات (إجباري) [0/2]         │
│ ┌────────┐ ┌────────┐ ┌────────┐   │
│ │فانيليا │ │شوكولاتة│ │فراولة │   │
│ └────────┘ └────────┘ └────────┘   │
│                                     │
│ 🍫 الصوصات (اختياري) [0/2]         │
│ ┌────────────┐ ┌────────────┐      │
│ │صوص شوكولاتة│ │صوص كراميل │      │
│ └────────────┘ └────────────┘      │
│                                     │
│ 🍪 الإضافات (اختياري) [0/3]        │
│ ┌────────┐ ┌────────┐ ┌────────┐   │
│ │أوريو   │ │لوتس   │ │سبرينكلز│   │
│ └────────┘ └────────┘ └────────┘   │
└─────────────────────────────────────┘
```

## 🔧 إذا لم يظهر:

### الحل 1: امسح الـ Cache
```
Ctrl + Shift + R
```

### الحل 2: افتح Incognito
```
Ctrl + Shift + N
ثم افتح: http://localhost:3000?product=soft_serve_cup
```

### الحل 3: اختبر الـ API مباشرة
```javascript
// في Console (F12)
fetch('https://softcream-api.mahmoud-zahran20025.workers.dev/products/soft_serve_cup/customization-rules?lang=ar')
  .then(r => r.json())
  .then(d => {
    console.log('API Response:', d)
    console.log('Groups:', d.data?.length)
  })
```

**Expected:** يجب أن يرجع 3 groups

## 📸 لقطة شاشة للـ Console

يجب أن ترى شيء مثل هذا:

```
🎨 Fetching customization rules for product soft_serve_cup...
   isOpen: true
   enabled: true
   URL: https://softcream-api.mahmoud-zahran20025.workers.dev/products/soft_serve_cup/customization-rules?lang=ar
✅ Loaded 3 customization groups
   Groups: (3) [{…}, {…}, {…}]
🔍 useCustomization State: {
  productId: "soft_serve_cup",
  isOpen: true,
  enabled: true,
  isLoading: false,
  hasError: false,
  rulesCount: 3,
  isCustomizable: true
}
🔍 ProductModal Debug: {
  productId: "soft_serve_cup",
  productName: "كوب سوفت سيرف مخصص",
  isCustomizable: true,
  rulesCount: 3,
  isLoadingRules: false
}
```

## ✅ إذا رأيت كل هذا:

**مبروك! 🎉** النظام يعمل بشكل كامل!

الآن يمكنك:
1. اختيار النكهات
2. إضافة الصوصات
3. إضافة التوبينج
4. رؤية السعر يتحدث
5. إضافة للسلة

---

**Server:** http://localhost:3000  
**Test URL:** http://localhost:3000?product=soft_serve_cup  
**الحالة:** ✅ جاهز للاختبار
