# 🚀 ابدأ من هنا - اختبار نظام BYO

## الخطوات السريعة

### 1. شغل الـ Server
```bash
cd soft-cream-nextjs
npm run dev
```

### 2. افتح صفحة الاختبار
```
http://localhost:3001/test-customization.html
```

هذه الصفحة ستختبر تلقائياً:
- ✅ هل الـ API يعمل؟
- ✅ هل المنتجات customizable؟
- ✅ هل القواعد موجودة؟

### 3. افتح المنتج
اختر أحد الروابط:

**فانيليا كلاسيك:**
```
http://localhost:3001?product=1
```

**كوب سوفت سيرف:**
```
http://localhost:3001?product=soft_serve_cup
```

### 4. افتح Developer Tools
```
اضغط F12
اذهب إلى Console
ابحث عن:
  🎨 Fetching customization rules...
  ✅ Loaded 3 customization groups
  🔍 ProductModal Debug: {...}
```

## ما يجب أن تراه

### في صفحة الاختبار:
```
✅ Product found: فانيليا كلاسيك
   is_customizable: 1
   ✓ Product is customizable!

✅ Found 3 customization groups

🍦 النكهات:
   - Required: No
   - Min/Max: 0-1
   - Options: 6

🍫 الصوصات:
   - Required: No
   - Min/Max: 0-2
   - Options: 5

🍪 الإضافات المقرمشة:
   - Required: No
   - Min/Max: 0-3
   - Options: 7

✓ API is working correctly!
✓ Now open the product in the website
```

### في الموقع (ProductModal):
```
┌─────────────────────────────────────┐
│ فانيليا كلاسيك                      │
│ 25 ج.م                              │
├─────────────────────────────────────┤
│ ✨ اصنع الآيس كريم الخاص بك         │
│                                     │
│ 🍦 النكهات (اختياري) [0/1]         │
│ ┌────────┐ ┌────────┐ ┌────────┐   │
│ │فانيليا │ │شوكولاتة│ │فراولة │   │
│ │مجاناً  │ │مجاناً  │ │مجاناً │   │
│ └────────┘ └────────┘ └────────┘   │
│                                     │
│ 🍫 الصوصات (اختياري) [0/2]         │
│ ┌────────────┐ ┌────────────┐      │
│ │صوص شوكولاتة│ │صوص كراميل │      │
│ │+5 ج.م      │ │+5 ج.م      │      │
│ └────────────┘ └────────────┘      │
│                                     │
│ 🍪 الإضافات (اختياري) [0/3]        │
│ ┌────────┐ ┌────────┐ ┌────────┐   │
│ │أوريو   │ │لوتس   │ │سبرينكلز│   │
│ │+8 ج.م  │ │+8 ج.م  │ │+4 ج.م  │   │
│ └────────┘ └────────┘ └────────┘   │
└─────────────────────────────────────┘
```

## إذا لم يظهر قسم التخصيص

### الحل السريع:
```bash
# في مجلد softcream-api
cd ../softcream-api
.\make-product-customizable.ps1 -ProductId "1"
```

### التحقق من المشكلة:
```
1. افتح http://localhost:3001/test-customization.html
2. اضغط "اختبار API" للمنتج
3. تحقق من النتائج
```

### إذا كان الـ API يعمل لكن الـ UI لا يعرض:
```
1. افتح Developer Tools (F12)
2. اذهب إلى Console
3. ابحث عن أخطاء
4. تحقق من:
   - 🎨 Fetching customization rules...
   - 🔍 ProductModal Debug: {...}
```

## الملفات المهمة

### للاختبار:
- `test-customization.html` - اختبار الـ API
- `DEBUG_CUSTOMIZATION.md` - دليل حل المشاكل
- `TESTING_GUIDE.md` - دليل الاختبار الكامل

### للتطوير:
- `src/components/modals/ProductModal/index.tsx` - المودال الرئيسي
- `src/components/modals/ProductModal/useCustomization.ts` - Hook التخصيص
- `src/components/modals/ProductModal/CustomizationSelector.tsx` - عرض الخيارات

## الأوامر المفيدة

### اختبار الـ API:
```bash
curl "https://softcream-api.mahmoud-zahran20025.workers.dev/products/1/customization-rules?lang=ar"
```

### جعل منتج customizable:
```powershell
cd softcream-api
.\make-product-customizable.ps1 -ProductId "2"
```

### التحقق من قاعدة البيانات:
```powershell
wrangler d1 execute soft_cream-orders-dev --remote --command "SELECT id, name, is_customizable FROM products WHERE is_customizable = 1"
```

## 🎯 الهدف

عند فتح أي منتج customizable، يجب أن:
1. ✅ يظهر قسم "اصنع الآيس كريم الخاص بك"
2. ✅ تظهر 3 مجموعات خيارات
3. ✅ يمكن اختيار الخيارات
4. ✅ السعر يتحدث تلقائياً
5. ✅ يمكن إضافة للسلة

## 📞 المساعدة

إذا واجهت مشاكل:
1. اقرأ `DEBUG_CUSTOMIZATION.md`
2. افتح `test-customization.html`
3. تحقق من Console في Developer Tools

---

**آخر تحديث:** 2025-11-27  
**الحالة:** ✅ جاهز للاختبار
