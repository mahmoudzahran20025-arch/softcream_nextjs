# 🧪 كيفية اختبار نظام BYO

## المشكلة الحالية
المنتج `soft_serve_cup` موجود في قاعدة البيانات ولكن قد لا يظهر في الـ Frontend.

## ✅ التحقق من وجود المنتج

### 1. اختبار الـ API مباشرة
افتح الملف `test-soft-serve-cup.html` في المتصفح:
```bash
# في مجلد soft-cream-nextjs
start test-soft-serve-cup.html
```

أو افتح هذه الروابط مباشرة:
- **جميع المنتجات:** https://softcream-api.mahmoud-zahran20025.workers.dev/products
- **المنتج المخصص:** https://softcream-api.mahmoud-zahran20025.workers.dev/products/soft_serve_cup
- **قواعد التخصيص:** https://softcream-api.mahmoud-zahran20025.workers.dev/products/soft_serve_cup/customization-rules?lang=ar

### 2. التحقق من قاعدة البيانات
```bash
wrangler d1 execute soft_cream-orders-dev --remote --command "SELECT id, name, is_customizable FROM products WHERE id = 'soft_serve_cup'"
```

**النتيجة المتوقعة:**
```
id             │ name                │ is_customizable
soft_serve_cup │ كوب سوفت سيرف مخصص  │ 1
```

## 🔍 لماذا لا يظهر المنتج؟

### السبب المحتمل 1: المنتج غير موجود في seed.js الأصلي
الحل: تم إضافته في migration 0004_seed_byo_data.sql ✅

### السبب المحتمل 2: الـ Frontend يستخدم cache قديم
الحل:
```bash
# امسح cache المتصفح
# أو افتح في Incognito mode
# أو اضغط Ctrl+Shift+R للتحديث القوي
```

### السبب المحتمل 3: المنتج لا يظهر في الفئات
الحل: المنتج في فئة "سوفت سيرف" - تأكد من أن هذه الفئة معروضة

## 🚀 كيفية الاختبار

### الطريقة 1: من الصفحة الرئيسية
```bash
1. cd soft-cream-nextjs
2. npm run dev
3. افتح http://localhost:3000
4. ابحث في شريط البحث عن "سوفت سيرف" أو "soft serve"
5. يجب أن يظهر المنتج "كوب سوفت سيرف مخصص"
```

### الطريقة 2: مباشرة عبر URL
```bash
1. npm run dev
2. افتح http://localhost:3000?product=soft_serve_cup
```

### الطريقة 3: من Console
```javascript
// افتح Developer Tools (F12)
// اذهب إلى Console
// اكتب:
window.location.href = '/?product=soft_serve_cup'
```

## 🎯 ما يجب أن تراه

### عند فتح المنتج:
1. ✅ صورة المنتج
2. ✅ الاسم: "كوب سوفت سيرف مخصص"
3. ✅ السعر: 45 ج.م
4. ✅ قسم "اصنع الآيس كريم الخاص بك" ✨
5. ✅ 3 مجموعات خيارات:
   - 🍦 النكهات (6 خيارات - مجاناً)
   - 🍫 الصوصات (5 خيارات - 5-8 ج.م)
   - 🍪 الإضافات المقرمشة (7 خيارات - 4-10 ج.م)

### إذا لم تر قسم التخصيص:
1. افتح Developer Tools (F12)
2. اذهب إلى Console
3. ابحث عن أخطاء
4. تحقق من:
   ```javascript
   // يجب أن ترى:
   🎨 Fetching customization rules for product soft_serve_cup...
   ✅ Loaded 3 customization groups
   ```

## 🐛 حل المشاكل الشائعة

### مشكلة 1: المنتج لا يظهر في القائمة
**الحل:**
```bash
# تحقق من أن المنتج موجود في API
curl https://softcream-api.mahmoud-zahran20025.workers.dev/products | grep soft_serve_cup

# إذا لم يظهر، أعد تشغيل seed:
cd softcream-api
echo "y" | wrangler d1 execute soft_cream-orders-dev --remote --file="migrations/0004_seed_byo_data.sql"
```

### مشكلة 2: قسم التخصيص لا يظهر
**الحل:**
```typescript
// تحقق من useCustomization hook
// في Console:
console.log('Product:', product)
console.log('is_customizable:', product?.is_customizable)
```

### مشكلة 3: API لا يستجيب
**الحل:**
```bash
# تحقق من أن Worker منشور
wrangler deploy

# اختبر API مباشرة
curl https://softcream-api.mahmoud-zahran20025.workers.dev/products/soft_serve_cup
```

### مشكلة 4: الخيارات لا تظهر
**الحل:**
```bash
# تحقق من الجداول
wrangler d1 execute soft_cream-orders-dev --remote --command "SELECT COUNT(*) FROM options"
# يجب أن يكون 18

wrangler d1 execute soft_cream-orders-dev --remote --command "SELECT COUNT(*) FROM option_groups"
# يجب أن يكون 3
```

## 📊 Checklist الاختبار

- [ ] المنتج موجود في قاعدة البيانات
- [ ] المنتج يظهر في API /products
- [ ] قواعد التخصيص تعمل في API
- [ ] المنتج يظهر في Frontend
- [ ] قسم التخصيص يظهر عند فتح المنتج
- [ ] الخيارات تظهر بشكل صحيح
- [ ] يمكن اختيار الخيارات
- [ ] السعر يتحدث عند الاختيار
- [ ] Validation يعمل
- [ ] يمكن إضافة المنتج للسلة

## 💡 نصائح

1. **استخدم Incognito Mode** لتجنب مشاكل الـ cache
2. **افتح Developer Tools** دائماً لمراقبة الأخطاء
3. **تحقق من Network Tab** لرؤية API requests
4. **استخدم test-soft-serve-cup.html** للاختبار السريع

## 🎉 إذا نجح الاختبار

يجب أن ترى:
- ✅ المنتج يظهر في القائمة
- ✅ قسم التخصيص يعمل
- ✅ يمكن اختيار الخيارات
- ✅ السعر يُحسب بشكل صحيح
- ✅ يمكن إضافة المنتج للسلة

**مبروك! 🎊 النظام يعمل بنجاح!**

---

**آخر تحديث:** 2025-11-27  
**الحالة:** ✅ جاهز للاختبار
