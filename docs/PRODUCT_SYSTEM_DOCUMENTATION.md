# 📦 نظام المنتجات الشامل - Soft Cream
## التوثيق الكامل للـ Backend و Frontend و Admin Panel

---

## 📋 نظرة عامة

تم بناء نظام منتجات متكامل وقوي يدعم:
- ✅ منتجات عادية (Standard Products)
- ✅ منتجات قابلة للتخصيص (BYO - Build Your Own)
- ✅ مقاسات متعددة (Sizes)
- ✅ حاويات متنوعة (Containers)
- ✅ قيم غذائية ديناميكية (Dynamic Nutrition)
- ✅ نظام تسعير مرن (Flexible Pricing)

---

## 🗄️ هيكل قاعدة البيانات (Database Schema)

### الجداول الرئيسية

```sql
-- 1. جدول المنتجات الأساسي
products (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  nameEn TEXT,
  category TEXT NOT NULL,
  categoryEn TEXT,
  price REAL NOT NULL,
  description TEXT,
  descriptionEn TEXT,
  image TEXT,
  badge TEXT,
  available INTEGER DEFAULT 1,
  
  -- نوع المنتج والتخصيص
  product_type TEXT DEFAULT 'standard',  -- standard, byo_ice_cream, milkshake, preset_ice_cream, dessert
  is_customizable INTEGER DEFAULT 0,
  
  -- القيم الغذائية
  calories INTEGER DEFAULT 0,
  protein REAL DEFAULT 0,
  carbs REAL DEFAULT 0,
  fat REAL DEFAULT 0,
  sugar REAL DEFAULT 0,
  fiber REAL DEFAULT 0,
  
  -- معلومات الطاقة
  energy_type TEXT DEFAULT 'none',  -- none, mental, physical, balanced
  energy_score INTEGER DEFAULT 0,
  
  -- بيانات إضافية (JSON)
  tags TEXT,
  ingredients TEXT,
  nutrition_facts TEXT,
  allergens TEXT
)

-- 2. مجموعات الخيارات (النكهات، الصوصات، الإضافات)
option_groups (
  id TEXT PRIMARY KEY,
  name_ar TEXT NOT NULL,
  name_en TEXT,
  description_ar TEXT,
  description_en TEXT,
  icon TEXT,
  display_order INTEGER DEFAULT 0
)

-- 3. الخيارات الفردية
options (
  id TEXT PRIMARY KEY,
  group_id TEXT REFERENCES option_groups(id),
  name_ar TEXT NOT NULL,
  name_en TEXT,
  description_ar TEXT,
  description_en TEXT,
  base_price REAL DEFAULT 0,
  image TEXT,
  available INTEGER DEFAULT 1,
  display_order INTEGER DEFAULT 0,
  
  -- القيم الغذائية
  calories REAL DEFAULT 0,
  protein REAL DEFAULT 0,
  carbs REAL DEFAULT 0,
  sugar REAL DEFAULT 0,
  fat REAL DEFAULT 0,
  fiber REAL DEFAULT 0
)

-- 4. ربط المنتجات بالخيارات (قواعد التخصيص)
product_options (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  product_id TEXT REFERENCES products(id),
  option_group_id TEXT REFERENCES option_groups(id),
  is_required INTEGER DEFAULT 0,
  min_selections INTEGER DEFAULT 0,
  max_selections INTEGER DEFAULT 1,
  price_override REAL,
  display_order INTEGER DEFAULT 0
)

-- 5. المقاسات
product_sizes (
  id TEXT PRIMARY KEY,
  name_ar TEXT NOT NULL,
  name_en TEXT,
  description_ar TEXT,
  description_en TEXT,
  price_multiplier REAL DEFAULT 1.0,
  volume_ml INTEGER,
  icon TEXT,
  display_order INTEGER DEFAULT 0
)

-- 6. الحاويات
container_types (
  id TEXT PRIMARY KEY,
  name_ar TEXT NOT NULL,
  name_en TEXT,
  description_ar TEXT,
  description_en TEXT,
  base_price REAL DEFAULT 0,
  image TEXT,
  icon TEXT,
  display_order INTEGER DEFAULT 0
)

-- 7. ربط المنتجات بالمقاسات
product_size_config (
  product_id TEXT REFERENCES products(id),
  size_id TEXT REFERENCES product_sizes(id),
  price_adjustment REAL DEFAULT 0,
  available INTEGER DEFAULT 1,
  PRIMARY KEY (product_id, size_id)
)

-- 8. ربط المنتجات بالحاويات
product_container_config (
  product_id TEXT REFERENCES products(id),
  container_id TEXT REFERENCES container_types(id),
  price_adjustment REAL DEFAULT 0,
  available INTEGER DEFAULT 1,
  PRIMARY KEY (product_id, container_id)
)
```

---

## 🔌 API Endpoints

### Products API (Public)

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/products` | جلب جميع المنتجات |
| GET | `/products/:id` | جلب منتج محدد |
| GET | `/products/:id/configuration` | جلب إعدادات التخصيص للمنتج |
| GET | `/products/:id/customization` | جلب خيارات التخصيص المتاحة |

### Admin API (Protected)

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/admin/products` | جلب جميع المنتجات (للأدمن) |
| POST | `/admin/products` | إنشاء منتج جديد |
| PUT | `/admin/products/:id` | تحديث منتج |
| DELETE | `/admin/products/:id` | حذف منتج |
| PUT | `/admin/products/:id/availability` | تغيير حالة التوفر |
| GET | `/admin/products/:id/configuration` | جلب إعدادات التخصيص |
| PUT | `/admin/products/:id/customization` | تحديث إعدادات التخصيص |

### BYO Options API (Admin)

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/admin/options` | جلب جميع الخيارات مجمعة |
| POST | `/admin/options` | إنشاء خيار جديد |
| PUT | `/admin/options/:id` | تحديث خيار |
| DELETE | `/admin/options/:id` | حذف خيار |
| GET | `/admin/sizes` | جلب المقاسات |
| GET | `/admin/containers` | جلب الحاويات |

---

## 🎨 Frontend Components

### هيكل المكونات

```
src/
├── components/
│   ├── admin/
│   │   ├── ProductsPage.tsx      # صفحة إدارة المنتجات
│   │   ├── BYOPage.tsx           # صفحة إدارة خيارات BYO
│   │   ├── AdminApp.tsx          # التطبيق الرئيسي للأدمن
│   │   └── Sidebar.tsx           # القائمة الجانبية
│   │
│   ├── modals/
│   │   └── ProductModal/
│   │       ├── index.tsx                    # المودال الرئيسي
│   │       ├── useProductLogic.ts           # منطق المنتج
│   │       ├── useCustomization.ts          # منطق التخصيص
│   │       ├── SizeSelector.tsx             # اختيار المقاس
│   │       ├── ContainerSelector.tsx        # اختيار الحاوية
│   │       ├── CustomizationSelector.tsx    # اختيار التخصيصات
│   │       ├── CustomizationSummary.tsx     # ملخص التخصيص
│   │       ├── NutritionInfo.tsx            # القيم الغذائية
│   │       ├── ActionFooter.tsx             # أزرار الإجراءات
│   │       └── templates/
│   │           ├── index.ts                 # تصدير القوالب
│   │           └── ProductTemplateRenderer.tsx
│   │
│   └── wizard/
│       └── BYOWizard.tsx         # معالج BYO خطوة بخطوة
│
├── hooks/
│   └── useProductConfiguration.ts  # Hook لجلب إعدادات المنتج
│
├── lib/
│   ├── api.ts                    # API العام
│   └── adminApi.ts               # API الأدمن
│
└── providers/
    └── CartProvider.tsx          # مزود السلة
```


---

## 🔄 تدفق البيانات (Data Flow)

### 1. عرض المنتج للعميل

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│   ProductCard   │────▶│   ProductModal   │────▶│   CartProvider  │
│   (عرض المنتج)   │     │   (تفاصيل+تخصيص)  │     │   (إضافة للسلة)  │
└─────────────────┘     └──────────────────┘     └─────────────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │ useProductConfiguration │
                    │   (جلب الإعدادات)      │
                    └──────────────────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │   API: /products/:id  │
                    │   /configuration      │
                    └──────────────────────┘
```

### 2. إدارة المنتج من الأدمن

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  ProductsPage   │────▶│  Config Modal    │────▶│   Admin API     │
│  (قائمة المنتجات) │     │  (إعدادات التخصيص) │     │   (حفظ البيانات) │
└─────────────────┘     └──────────────────┘     └─────────────────┘
        │
        ▼
┌─────────────────┐
│    BYOPage      │
│ (إدارة الخيارات) │
└─────────────────┘
```

---

## 📊 أنواع المنتجات (Product Types)

| النوع | الوصف | الميزات |
|-------|-------|---------|
| `standard` | منتج عادي | سعر ثابت، بدون تخصيص |
| `byo_ice_cream` | آيس كريم قابل للتخصيص | نكهات، صوصات، إضافات، مقاسات، حاويات |
| `milkshake` | ميلك شيك | نكهات، إضافات، مقاسات |
| `preset_ice_cream` | آيس كريم جاهز | وصفة محددة، مقاسات فقط |
| `dessert` | حلويات | تخصيص محدود |

---

## 💰 نظام التسعير المرن

### حساب السعر النهائي

```typescript
السعر النهائي = السعر الأساسي 
              + (سعر المقاس × معامل المقاس)
              + سعر الحاوية
              + مجموع أسعار الخيارات المختارة
```

### مثال عملي

```
منتج: كوب آيس كريم مخصص
├── السعر الأساسي: 25 ج.م
├── المقاس (كبير): +10 ج.م
├── الحاوية (كوب وافل): +5 ج.م
├── النكهات:
│   ├── فانيليا: 0 ج.م (مجاني)
│   └── شوكولاتة: 0 ج.م (مجاني)
├── الصوص (كراميل): +3 ج.م
└── الإضافات:
    ├── لوتس: +5 ج.م
    └── مكسرات: +7 ج.م
────────────────────────────
السعر النهائي: 55 ج.م
```

---

## 🥗 نظام القيم الغذائية الديناميكي

### حساب القيم الغذائية

```typescript
interface NutritionInfo {
  calories: number;
  protein: number;
  carbs: number;
  sugar: number;
  fat: number;
  fiber: number;
}

// الحساب الديناميكي
const totalNutrition = {
  calories: baseCalories + selectedOptions.reduce((sum, opt) => sum + opt.calories, 0),
  protein: baseProtein + selectedOptions.reduce((sum, opt) => sum + opt.protein, 0),
  // ... وهكذا لباقي القيم
};
```

---

## 🛠️ دليل الاستخدام للأدمن

### 1. إضافة منتج جديد

1. افتح صفحة **المنتجات** من القائمة الجانبية
2. اضغط **إضافة منتج**
3. املأ البيانات الأساسية:
   - معرف المنتج (ID)
   - الاسم بالعربية والإنجليزية
   - الفئة
   - السعر
   - الوصف
   - رابط الصورة
4. أضف القيم الغذائية (اختياري)
5. اضغط **حفظ المنتج**

### 2. تفعيل التخصيص للمنتج

1. من قائمة المنتجات، اضغط على زر ⚙️ (إعدادات التخصيص)
2. اختر **نوع المنتج**:
   - BYO آيس كريم ✨
   - ميلك شيك 🥤
   - آيس كريم جاهز 🍨
   - حلويات 🍰
3. سيتم تفعيل التخصيص تلقائياً

### 3. إدارة خيارات BYO

1. افتح صفحة **خيارات BYO** من القائمة الجانبية
2. ستجد المجموعات:
   - 🍦 النكهات
   - 🍫 الصوصات
   - 🍪 الإضافات
   - 🥜 التوبينج
   - 🍓 الفواكه
3. لإضافة خيار جديد:
   - اضغط **إضافة خيار جديد**
   - اختر المجموعة
   - أدخل الاسم والسعر والقيم الغذائية
4. لتعديل خيار: اضغط على أيقونة التعديل ✏️
5. لحذف خيار: اضغط على أيقونة الحذف 🗑️

---

## 🔧 Backend Services

### customizationService.js

```javascript
// الدوال الرئيسية
export async function getProductConfiguration(productId, env, lang) {
  // جلب إعدادات المنتج الكاملة
  // - معلومات المنتج
  // - المقاسات المتاحة
  // - الحاويات المتاحة
  // - قواعد التخصيص
}

export async function getProductCustomizationOptions(productId, env, lang) {
  // جلب خيارات التخصيص المتاحة للمنتج
  // - مجموعات الخيارات
  // - الخيارات الفردية مع الأسعار والقيم الغذائية
}

export async function calculateCustomizationPrice(selections, env) {
  // حساب السعر الإجمالي للتخصيصات المختارة
}

export async function calculateCustomizationNutrition(selections, env) {
  // حساب القيم الغذائية الإجمالية
}
```

---

## 📱 تجربة المستخدم (UX Flow)

### رحلة العميل لطلب منتج مخصص

```
1. 🏠 الصفحة الرئيسية
   └── عرض المنتجات حسب الفئات

2. 🍦 اختيار المنتج
   └── فتح ProductModal

3. 📏 اختيار المقاس (إن وجد)
   └── SizeSelector

4. 🥤 اختيار الحاوية (إن وجد)
   └── ContainerSelector

5. ✨ التخصيص (إن كان متاحاً)
   ├── اختيار النكهات
   ├── اختيار الصوص
   └── اختيار الإضافات

6. 📊 مراجعة الملخص
   ├── CustomizationSummary
   └── NutritionInfo

7. 🛒 إضافة للسلة
   └── CartProvider.addItem()

8. 💳 إتمام الطلب
   └── CheckoutModal
```

---

## 🚀 الاقتراحات والتحسينات المستقبلية

### قريباً (Short-term)

1. **نظام الصور للخيارات**
   - رفع صور للنكهات والإضافات
   - عرض الصور في واجهة الاختيار

2. **نظام العروض على التخصيصات**
   - خصم على مجموعات معينة
   - عروض "اختر 3 بسعر 2"

3. **تحسين الـ Admin Panel**
   - Drag & Drop لترتيب الخيارات
   - معاينة مباشرة للمنتج

### متوسط المدى (Mid-term)

4. **نظام الوصفات الجاهزة**
   - حفظ تخصيصات شائعة كوصفات
   - "الأكثر طلباً" للتخصيصات

5. **تحليلات التخصيص**
   - أكثر النكهات طلباً
   - أفضل التركيبات

6. **نظام التوصيات**
   - "عملاء آخرون أضافوا..."
   - توصيات بناءً على الاختيارات

### طويل المدى (Long-term)

7. **تطبيق موبايل مخصص**
   - تجربة BYO محسنة للموبايل
   - حفظ التفضيلات

8. **نظام الولاء**
   - نقاط على التخصيصات
   - مكافآت للعملاء المتكررين

9. **AI للتوصيات**
   - توصيات ذكية بناءً على التاريخ
   - تخصيصات موسمية

---

## 📁 الملفات المهمة

### Backend (softcream-api)

```
src/
├── routes/
│   ├── admin.js          # جميع endpoints الأدمن
│   └── products.js       # endpoints المنتجات العامة
├── services/
│   ├── customizationService.js  # خدمة التخصيص
│   └── orderService.js          # خدمة الطلبات
└── migrations/
    ├── 0003_add_byo_tables.sql
    ├── 0004_seed_byo_data.sql
    ├── 0005_add_nutrition_to_options.sql
    ├── 0007_add_sizes_and_containers.sql
    ├── 0008_seed_sizes_containers.sql
    └── 0009_restructure_products.sql
```

### Frontend (soft-cream-nextjs)

```
src/
├── components/admin/
│   ├── ProductsPage.tsx
│   └── BYOPage.tsx
├── components/modals/ProductModal/
│   ├── index.tsx
│   ├── useCustomization.ts
│   └── ...
├── hooks/
│   └── useProductConfiguration.ts
└── lib/
    └── adminApi.ts
```

---

## ✅ الخلاصة

تم بناء نظام منتجات متكامل يتميز بـ:

- **المرونة**: دعم أنواع متعددة من المنتجات
- **القابلية للتوسع**: سهولة إضافة خيارات جديدة
- **الأداء**: تحميل ذكي للبيانات
- **تجربة المستخدم**: واجهة سلسة للتخصيص
- **إدارة سهلة**: لوحة تحكم شاملة للأدمن
- **القيم الغذائية**: حساب ديناميكي ودقيق

---

*آخر تحديث: نوفمبر 2025*

الملفات المنشأة:
1. soft-cream-nextjs/docs/PRODUCT_SYSTEM_DOCUMENTATION.md

نظرة عامة على النظام
هيكل قاعدة البيانات الكامل
جميع API Endpoints
هيكل مكونات الـ Frontend
تدفق البيانات (Data Flow)
أنواع المنتجات
نظام التسعير المرن
نظام القيم الغذائية الديناميكي
دليل استخدام الأدمن
Backend Services
تجربة المستخدم (UX Flow)
الاقتراحات والتحسينات المستقبلية
قائمة الملفات المهمة
2. softcream-api/docs/PRODUCT_API_REFERENCE.md

Base URL
Authentication
Products API (Public)
Admin Products API
BYO Options API
Sizes & Containers API
Testing Examples (PowerShell & cURL)
Error Responses
ملخص ما تم بناؤه:
| الميزة | الحالة | |--------|--------| | نظام المنتجات الأساسي | ✅ | | نظام BYO (Build Your Own) | ✅ | | المقاسات والحاويات | ✅ | | القيم الغذائية الديناميكية | ✅ | | نظام التسعير المرن | ✅ | | Admin Panel - المنتجات | ✅ | | Admin Panel - خيارات BYO | ✅ | | API Documentation | ✅ |